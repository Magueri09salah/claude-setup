# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
(Project pinned to SDK 54 on 2026-07-20 to match the Expo Go the owner's iPhone can install — its iOS version only gets the SDK 54 build. Upgrade the SDK only after confirming the phone's Expo Go supports it.)
