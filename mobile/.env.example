# Your computer's LAN IP + api port, so Expo Go on a phone can reach the API.
# Find it with: ipconfig (IPv4 Address). Restart `npx expo start` after changing.
EXPO_PUBLIC_API_URL=http://192.168.1.10:4000
